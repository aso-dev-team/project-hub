# リポジトリ運用ガイドライン（AI）

- **返答は日本語**で統一する。

---

## 質問フォーマット

スキルやルール内でユーザーへの質問を定義する場合は、以下の形式で統一する：

```markdown
**質問 A: ~~**

1. 選択肢 1or 質問 1
2. 選択肢 2or 質問 2

**質問 B: ~~**

1. 選択肢 1or 質問 1
2. 選択肢 2or 質問 2
3. 選択肢 3or 質問 3
```

回答とみなす例

- `a1b3c2`
- `A1B3C2`
- `A1 B2 C1`（スペース区切り）
- 改行区切り:

```text
A1
B2
C1
```

---

## 基本方針

--

## 追加で読むべきルールファイル

- read @.claude/rules/general.md (なければ読まなくて良い)
- read @.claude/rules/project-context.md (なければ読まなくて良い)
- read @AGENTS.local.md (なければ読まなくて良い)

---

## 厳守: ツール利用

- Vue プロジェクトのパッケージ管理は `pnpm` を利用すること

---

## コード検索の推奨

- ユーザーが `@path/to/file` でファイルパスを指定した場合は、まず read でそのファイルを読み込む。
- 定義ジャンプや参照解決が必要なときは Serena MCP を推奨（ファイルパスが不明確な場合に特に有効）。

---

## パス別適用ルール

作業対象のファイルパスに応じて、以下のルールファイルを参照してください。

| 対象パス | 参照すべきルールファイル |
| -------- | ------------------------ |
|          |                          |

**注**: パスが複数のルールに該当する場合は、すべての該当ルールを適用してください。

---

## プロジェクト構成（配置の目安）

- `src/` : **ASP.NET Core 8 バックエンド**（Controllers/Services、EF Core の `Data/`、Razor の `Views/`、静的ファイル `wwwroot/` など）
- `vue/` : **Vue 3 + Vite フロントエンド**（エントリポイントは `vue/src/main.ts`）
- `views/` : ホスト側の **legacy Razor pages**
- `vue/dist/` : ビルド後の SPA アセット配置先
- `shell/` : EF Core 補助スクリプト
- `docs/` : 仕様・UX 関連ドキュメント
- `ci/` : CI/CD 用のアイテム置き場
- `aws/` : aws 用のアイテム置き場
